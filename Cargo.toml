# SPDX-License-Identifier: AGPL-3.0-only

[package]
name = "rust-exec"
edition = "2024"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
